# Supabase Configuration
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Mode LAN (optionnel - pour répétitions offline)
VITE_LAN_WS_URL=ws://localhost:8787

# Notes:
# - Copier ce fichier en .env et remplir les valeurs
# - Ne JAMAIS commit le fichier .env (contient les secrets)
# - Pour le mode LAN: lancer `pnpm dev:lan` dans un terminal séparé
